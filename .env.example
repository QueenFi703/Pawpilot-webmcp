# Development Environment Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000
NODE_ENV=development
# Server-side OpenAI credential used by /api/agent. Never expose this in client code.
OPENAI_API_KEY_PawPilot=
OPENAI_MODEL=gpt-5.6-luna
# Used to sign short-lived care-plan approval tokens. Use a long random value in production.
PAWPILOT_APPROVAL_SECRET=
